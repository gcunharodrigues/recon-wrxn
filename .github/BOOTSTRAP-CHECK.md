# wrxn-ci registration (gate-06 bootstrap)

Throwaway PR to make the `wrxn-ci` required status check report once on recon-wrxn before the `wrxn-main-gate` ruleset (no bypass actor) is applied. Safe to close without merging.
